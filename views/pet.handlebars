<div class="text-center">
  <h2>{{{name}}}</h2>
  <p>{{{sex}}}</p>
  <p>{{{age}}}</p>
  <p>{{{species}}}</p>
  <p>{{{breed}}}</p>
  <p>{{{description}}}</p>
  <p>
    {{!-- img --}}
  </p>
  <p>
    <button class="btn btn-primary">Adopt This Pet!/button>
  </p>
</div>
